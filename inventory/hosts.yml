---
all:
  children:
    workstations:
      hosts:
        localhost:
          ansible_connection: local
      # Add remote workstations here, e.g.:
      # my_laptop:
      #   ansible_host: 192.168.1.10
      #   ansible_user: balevision
    servers:
      hosts: {}
      # Add remote servers here, e.g.:
      # dev_server:
      #   ansible_host: 192.168.1.20
      #   ansible_user: admin
