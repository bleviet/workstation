---
# Defaults for all workstations (desktops / laptops).
# Override in host_vars/<hostname>.yml for per-machine settings.

profile: desktop-xfce

features:
  editor:
    neovim: true
    lazyvim: false
    vscode: false
  fpga:
    vivado: false
    quartus: false
    oss: false
  xrdp: false
