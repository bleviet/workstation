---
# Defaults for all servers (headless remotes).
# Override in host_vars/<hostname>.yml for per-machine settings.

profile: headless

features:
  fpga:
    vivado: false
    quartus: false
    oss: false
  xrdp: true
