# Enforce LF line endings for all text files on checkout.
* text=auto eol=lf

# Dotfiles must always be LF — Windows editors can produce CRLF here.
dotfiles/**  text eol=lf

# Explicitly binary — never touch line endings.
*.png        binary
*.jpg        binary
*.jpeg       binary
*.gif        binary
*.ico        binary
